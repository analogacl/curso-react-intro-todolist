import React from 'react';
import { TodoIcon } from '.';

function CompleteIcon() 
{
    return (
        <TodoIcon
        // type={ completed ? 'check' : 'pendiente' }
        // onClick={ onComplete }
        />
    );
}

export  { CompleteIcon };